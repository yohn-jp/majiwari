{
  "name": "@majiwari/gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Generic stdio MCP to Streamable HTTP transport gateway. No OCR-specific or adapter-specific logic. Thin wrapper around mcp-proxy (https://github.com/punkpeye/mcp-proxy).",
  "scripts": {
    "start": "node bin/gateway.mjs",
    "check": "node --check bin/gateway.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "mcp-proxy": "6.7.10"
  },
  "engines": {
    "node": ">=20"
  }
}
