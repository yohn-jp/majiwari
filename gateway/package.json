{
  "name": "@majiwari/gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Generic stdio MCP to Streamable HTTP transport gateway. No OCR-specific or adapter-specific logic. Thin wrapper around mcp-proxy (https://github.com/punkpeye/mcp-proxy).",
  "main": "src/index.js",
  "scripts": {
    "start": "node bin/gateway.mjs",
    "test": "node --test test/*.test.js",
    "check": "node --check bin/gateway.mjs && node --check src/index.js && node --check src/gateway-transport.js && node --check src/registry-gateway.js && node --check src/stdio-target.js"
  },
  "dependencies": {
    "@majiwari/registry": "^0.1.0",
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "mcp-proxy": "6.7.10"
  },
  "engines": {
    "node": ">=22.13"
  }
}
